services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: kb-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
